# 🐱 猫咪游戏运行指南

## 快速启动

### 方法1：使用启动脚本（推荐）
```bash
./start_game.sh
```

### 方法2：手动启动
```bash
# 安装依赖（如果还没安装）
npm install

# 启动完整游戏（前端+后端）
npm run dev:full
```

### 方法3：分别启动
```bash
# 终端1：启动后端服务器
npm run dev:server

# 终端2：启动前端开发服务器
npm run dev
```

## 访问游戏

- **游戏地址**: http://localhost:5173
- **后端API**: http://localhost:3000

## 常见问题

### 1. 背包不显示物品
**原因**: 只运行了前端，没有后端服务器
**解决**: 使用 `npm run dev:full` 或 `./start_game.sh`

### 2. 叼走鱼后背包没有鱼
**原因**: 物品ID不匹配（已修复）
**解决**: 重新启动游戏

### 3. 存档功能不工作
**原因**: 后端服务器未启动
**解决**: 确保同时运行前端和后端

### 4. 端口被占用
**解决**: 
```bash
# 查看端口占用
lsof -i :5173
lsof -i :3000

# 杀死占用进程
kill -9 <进程ID>
```

## 调试功能

在浏览器控制台中可以使用以下调试命令：

```javascript
// 添加小鱼干到背包
debugAddFish()

// 清空背包
debugClearInventory()

// 显示成就提示
debugShowAchievement()
```

## 游戏功能

- 🐱 **猫咪行为**: 睡觉、玩耍、探索、破坏
- 🎯 **成就系统**: 解锁各种成就
- 💾 **存档系统**: 保存和加载游戏进度
- 🎨 **美术资源**: 程序化生成的图形
- 🎮 **交互系统**: 物品与环境的互动

## 开发模式

```bash
# 只运行前端（用于UI开发）
npm run dev

# 只运行后端（用于API开发）
npm run dev:server

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

## 文件结构

```
cat_not_easy_phaser/
├── src/                    # 源代码
│   ├── scenes/            # 游戏场景
│   ├── systems/           # 游戏系统
│   └── utils/             # 工具类
├── public/                # 静态资源
│   └── assets/           # 游戏资源
├── server/               # 后端服务器
├── doc/                  # 文档
└── temp/                 # 临时文件（存档等）
```

## 技术栈

- **前端**: Phaser 3 + TypeScript + Vite
- **后端**: Node.js + Express + TypeScript
- **数据**: JSON配置文件
- **存档**: 本地文件系统

享受游戏吧！🐱✨ 